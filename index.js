const generateRandomNumbers = require('./lib/random-numbers.js');
const generateRandomLetters = require('./lib/random-letter.js');

module.exports = {
  generateRandomNumbers
  generateRandomLetters
}